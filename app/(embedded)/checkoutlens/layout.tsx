"use client";

// Checkout Lens embedded app layout.
// NavMenu wired per README conventions.
export default function CheckoutLensLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
